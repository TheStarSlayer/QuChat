/**
 * Makes the whole page uninteractive (modal) with only a message (User already logged in, use the original session or logout from original session)
 */

function ExitPageWarning() {

}

export default ExitPageWarning;